# The Ours vs Theirs Confusion During Rebase

During rebase, the meaning is REVERSED from merge:
- `--ours` = the base branch (main)
- `--theirs` = your commits being replayed

Memory aid: "Rebase Reverses"
